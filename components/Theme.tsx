import { createTheme } from '@mui/material/styles'

export default createTheme({
  palette: {
    primary: {
      main: '#2fce4c',
      contrastText: '#ffffff',
    },
    secondary: {
      main: '#00cc27',
      contrastText: '#ffffff',
    },
  },
})

